const ROUTES = Object.freeze({
  LANDING: '/',
  HOME: '/home',
  DETAIL: '/detail/:countryID',
  FORM: '/form'
})

const API = Object.freeze({
  DOMAIN: 'http://localhost:3001'
})

export {
  ROUTES,
  API
}
